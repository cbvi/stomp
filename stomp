#!/usr/bin/env perl6
use v6;
use lib '.';
use Stomp::CLI;

Stomp::Client.Usage() if not @*ARGS[0];

my Str $command = @*ARGS.shift;
my Str @options = @*ARGS;

Stomp::CLI.Command($command, @options);

# vim: ft=perl6
