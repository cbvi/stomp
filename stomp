#!/usr/bin/env perl6
use v6;
use lib 'lib';
use Stomp::CLI;
use Stomp::Utils;

if not Stomp::Utils::IsSetup() {
    Stomp::CLI::Setup();
}

Stomp::CLI.Usage() if not @*ARGS[0];

my Str $command = @*ARGS.shift;
my Str @options = @*ARGS;

Stomp::CLI.Command($command, @options);

# vim: ft=perl6
